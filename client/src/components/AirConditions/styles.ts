import { makeStyles } from "tss-react/mui";

export const useAirConditionsStyles = makeStyles()(() => ({
  container: {
    borderRadius: "20px",
    backgroundColor: "#EAECEF",
    padding: "24px",
    userSelect: "none",
  },
}));
